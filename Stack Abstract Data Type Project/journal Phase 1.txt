
Journal Phase 2

If the user calls undo() when no changes have been made, there is nothing to undo. I handled this by checking if the undoStack is empty before removing anything from it. If the stack is empty, the method simply does nothing instead of causing an error.
